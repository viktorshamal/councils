.container
  .row
    .col-md-6.col-md-offset-3{"ng-repeat" => "paragraph in document.paragraphs"}
      .card.meeting-card
        .card-block.card-primary.text-center
          %p.card-text(editable-text = "paragraph.description")
            {{paragraph.id}}. {{ paragraph.description || "empty" }}
%hr/